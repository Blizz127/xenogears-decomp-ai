# W34 overnight audit — frontier 0x80071490

Date: 2026-08-23
Retail authority: `disc/world_map.bin`, load base `0x8006FAF0`, SHA-256
`4c15fd32b3a03d7cd5ea4403dcaabc70abaf99b6aaca65d63d6866803edaac70`.

## Runtime frontier

The natural prologue diagnostic reaches `0x800712D0`, executes the second
`0x80097800` scheduler pass with 29 callbacks / 0 missing, and stops at the
retail `DrawSync` call at `0x80071490`. The ordinary scheduler proof remains
the archived one-pass 16/16 proof; the prologue diagnostic is the proof of
the second-pass hard cut. Mode-loop `0x80072238` and renderer entry
`0x8007299C` are still zero-hit.

## Retail region and JAL inventory

The containing function is `[0x800712D0, 0x80071A50)`, 368 instructions
from the current frontier to the return. The smallest bounded continuation
is `[0x80071490, 0x800714D4)`, 17 instructions, ending immediately before
the first gated branch at `0x800714D4`:

| PC | Target | Retail operation | Host status |
|---|---|---|---|
| `0x80071490` | `0x800445D0` | `DrawSync(0)` | PsyCross shim/export available |
| `0x80071498` | `0x8004B54C` | `VSync(2)` | PsyCross export available |
| `0x800714A0` | `0x80019CA0` | `GameCheckAndHandleSoftReset()` | linked host function available |
| `0x800714B0` | `0x80044E9C` | `PutDispEnv(*(BE3C+0x5C))` | host API available; guest pointer must map |
| `0x800714C0` | `0x80044C44` | `PutDrawEnv(*BE3C)` | host API available; guest pointer must map |

All five callees are already ported/linked. There are no loops, indirect
calls, or new scheduler passes in this bounded region. The touched guest
state is `0x8009BE3C` (current draw-environment pointer), plus the two
environment records at `BE3C` and `BE3C+0x5C`; no stores are made by the
retail caller. The two `Put*Env` arguments are guest PSX addresses at retail
and must be converted with `PSX_ADDR` before entering host code.

The existing `wm_800712D0_frame_prologue` already owns the preceding
`0x80071488` scheduler call and has the correct sync/wait shims. This slice
extends that same function and moves its hard cut to `0x800714D4`.

## Remainder audit / boundary

The remaining `[0x800714D4, 0x80071A50)` region is 351 instructions and
contains a long gated update chain. Its JAL inventory from the retail audit
is:

`0x80071578 -> 0x80093F18`, `0x80071684 -> 0x80075D4C`,
`0x80071704 -> 0x8007634C`, `0x8007175C -> 0x80035734`,
`0x8007176C -> 0x80076594`, `0x800717FC -> 0x80075E7C`,
`0x800718E8 -> 0x800758C0`, `0x8007190C/0x8007191C -> 0x800762FC`,
`0x80071914 -> 0x8001C634`, `0x80071924 -> 0x80075B58`,
`0x8007197C -> 0x80025044`, `0x80071984 -> 0x80074F2C`,
`0x8007198C -> 0x80075104`, `0x8007199C -> 0x8004A12C`,
`0x800719B4 -> 0x80044BD0 (DrawOTag)`, `0x800719D0 -> 0x80044110`,
`0x80071A08 -> 0x8004495C`, `0x80071A10 -> 0x80096694`,
`0x80071A18 -> 0x800445D0 (DrawSync)`, `0x80071A20 -> 0x8004B54C (VSync)`,
`0x80071A30 -> 0x80044E9C (PutDispEnv)`.

That remainder exceeds the 150-instruction bound and includes several
unported overlay helpers plus the renderer-side `DrawOTag`; it is
`BLOCKED-NEEDS-REVIEW` for this slice. After the bounded continuation is
accepted, the next audit must classify the branch/gated update chain rather
than implement through it speculatively.

## Classification and smallest implementation

Class (c)/(d): host-boundary pointer mapping plus existing DrawSync/VSync
and soft-reset/display-environment host paths. Implement only the five calls
through `0x800714C4`, then report frontier `0x800714D4`.
