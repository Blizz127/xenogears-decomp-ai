# W34 overnight audit — retail `0x8007185C`

## Frontier and bounded region

Slice 03 leaves the natural route at `0x8007185C`. The complete retail
region `[0x8007185C, 0x8007197C)` is 288 bytes / 72 instructions. Its
natural lane is the flag clear/toggle followed by a fresh `C178` test and
the `D804=0` store at `0x80071978`; the next unresolved call is at
`0x8007197C`.

## Natural control flow and state

Retail first loads the unsigned halfword at `0x8009BD10`, stores zero to
`0x8009D80C`, and, when `BD10 & 0x100` is set, toggles the halfword at
`0x8007EE76` with `xori 1`. It then freshly loads `0x8009C178`:

```text
0x80071890  lw   v0, 0x8009C178
0x8007189C  bnez v0, 0x80071974
0x80071974  sw   zero, 0x8009D804
0x8007197C  jal  0x80025044   # next frontier, outside this slice
```

The natural capture has `C178=1`, so the natural path executes the D80C
clear, conditionally the EE76 toggle, then the D804 clear and reaches
`0x8007197C` without a helper call. The alternate `C178==0` path checks
fresh `D804`, `D554`, and signed `BE10`; it can reach the call at
`0x800718E8` for `1 <= BE10 < 4`, or fall through to `0x8007197C`.

Touched state includes `BD10` (halfword), `D80C`, `EE76` (halfword),
`C178`, `D804`, `D554`, `BE10` (signed word), `0x80069460`, `0x80069178`,
`0x80069171`, `0x8007EE68`, `0x8009D7CC`, `0x8009D7D8`, and
`0x8007EE68`. The alternate `BE10 >= 4` lane also reads/writes
`0x8007EE68` and `0x8009D55C`.

## JAL inventory

| Call PC | Target | Status | Retail size / note |
|---|---|---|---|
| `0x800718E8` | `0x800758C0` | absent from the production port | 164 instructions to `jr ra`; large setup helper |
| `0x8007190C` | `0x800762FC` | partial unrelated static helper exists in `world_map_init.c`; no frame-driver mapping | 19 instructions, but it calls several unported resident helpers |
| `0x80071914` | `0x8001C634` | no production-linked `MenuMain` mapping for this call site | external/main executable |
| `0x8007191C` | `0x800762FC` | same partial/unresolved status | repeated call |
| `0x80071924` | `0x80075B58` | absent from the production port | 124 instructions to `jr ra`; setup helper |

The natural lane reaches none of these calls. The first alternate unresolved
call is `0x800718E8`; this audit does not cross it or rely on the approximate
stubs in `world_map_frame_driver_712d0.c`.

## Existing coverage and classification

No precise frame-driver source currently covers this region. The existing
`wm_800762FC` in `world_map_init.c` is a separate static implementation and
does not establish a valid mapping for the retail call site. Convergence and
common-tail pieces are downstream and do not cover these state gates.

Class (a) for the natural lane: implement the exact BD10/D80C/EE76 flag
operations, fresh C178 branch, and D804 clear, while holding the
call-bearing alternate lane at `0x800718E8`. The smallest natural advance is
therefore `0x8007185C -> 0x8007197C`.
