# W34 overnight audit — frontier 0x800714D4

Retail authority is unchanged: `disc/world_map.bin`, load base
`0x8006FAF0`, SHA-256 `4c15fd32b3a03d7cd5ea4403dcaabc70abaf99b6aaca65d63d6866803edaac70`.

## Bounded region

The smallest natural continuation is `0x800714D4..0x8007169C`, 114
instructions (456 bytes), ending at the common branch-chain entry after the
`0x80071694`/`0x80071698` `BD34=0` store. The retail code performs fresh
loads and branches on:

| PC range | Guest state / operation |
|---|---|
| `0x800714D4..0x800714D8` | `lbu 0x80069179`; nonzero → `0x80071694` |
| `0x800714DC..0x80071500` | `BD34`, `C178`, `D804`; zero/nonzero gates → `0x80071694` |
| `0x80071504..0x8007153C` | `D804`, signed `BD24`, signed `CE68`; gate → `0x80071694` |
| `0x80071540..0x80071564` | `D554`, `D80C`; gate → `0x80071694` |
| `0x80071568..0x80071574` | store `BD34=0`, prepare `a0=0x8009D55C` |
| `0x80071578` | first call beyond this bounded slice: `jal 0x80093F18` |
| `0x80071694..0x80071698` | common branch target, store `BD34=0` |
| `0x8007169C` | next state-dependent branch chain |

There are no loops or indirect calls in this bounded region. The only JAL
inside it is `0x80071578 -> 0x80093F18`; the helper is ported and is 51
instructions, but this slice deliberately stops before its call so both
branch outcomes are represented without speculating into the next family.
The exact implementation must use fresh reloads, signed halfword comparisons
for `BD24`/`CE68`, and a 32-bit store to `BD34`.

On the natural fixture, `BD34=0` after mode entry, so retail branches to
`0x80071694`, stores zero, and reaches the new frontier `0x8007169C`.

## Existing coverage and next boundary

No current production frame-driver source covers this caller region. The
approximate `world_map_frame_driver_712d0.c` contains a broad, non-authority
orchestration copy and is not reused. `wm_80093F18` is already linked, but
the next slice must audit its caller contract if the all-gates-pass path is
made live. The post-`0x8007169C` region has another bounded branch family
before the next calls at `0x80071704`, `0x8007175C`, and `0x8007176C`.

## Classification

Class (a): bounded branch/state leaf with no required callee when stopping
before `0x80071578`; implement only through the common `BD34` store and
report `0x8007169C` on the natural route.
