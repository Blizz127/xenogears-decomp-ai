# W34 overnight audit — retail `0x8007169C`

## Frontier and bounded region

The natural frontier after slice 02 is `0x8007169C`. The next direct
natural branch is the first eight retail instructions:

```text
0x8007169C  lw   v0, 0x8009C178
0x800716A0  nop
0x800716A8  bnez v0, 0x8007185C
0x800716AC  nop
```

The complete retail region `[0x8007169C, 0x8007185C)` is 176 bytes / 112
instructions. It is a branch-only state machine on the natural lane, but
its alternate lanes contain four calls and should not be crossed by a
summary implementation.

## State and control flow

The mode-entry evidence in `slice_02_natural.log` records
`0x8009C178 = 1`. Therefore the natural path takes the first `bnez` at
`0x800716A8` directly to `0x8007185C`, with no call or store in this
region. The next region begins by clearing `0x8009D80C` and testing the
`BD10 & 0x100` mode bit.

If `C178 == 0`, retail continues through fresh loads of `D804`, `D554`,
`D80C`, and `BD10`. The later lane rechecks `C178`, then `D804`, signed
`BD24 == -1`, `CE68 == BD24`, `D554`, and `D80C`. It can call the helpers
listed below or fall through to `0x8007185C`. The region also reads
`0x8007EF64` and `0x8007F8E5..0x8007F8E7`, and can store `D554`, `D7CC`,
`0x8006954C`, and `0x8007EE70..0x8007EE74` after the `0x80075E7C` call.

## JAL inventory

| Call PC | Target | Status | Retail size / note |
|---|---|---|---|
| `0x80071704` | `0x8007634C` | absent from the production port | 146 instructions to `jr ra`; overlay helper |
| `0x8007175C` | `0x80035734` | no production-linked port symbol found | resident `ControllerGetType`-class external; not on natural lane |
| `0x8007176C` | `0x80076594` | absent from the production port | approximately 143 instructions to `jr ra`; overlay helper |
| `0x800717FC` | `0x80075E7C` | absent from the production port | approximately 134 instructions to `jr ra`; calls `0x80093F18` |

None of these calls is reached when the captured natural `C178` value is
one. Implementing any alternate lane would cross an absent helper larger
than the 30-instruction leaf allowance, and would be class (e) without a
separate approved audit.

## Existing coverage

`world_map_frame_driver.c` precisely covers through the common
`BD34=0` store at `0x80071698`; it has no implementation for this region.
The older `world_map_frame_driver_712d0.c` contains approximate stubs and
raw guest-address patterns and is not coverage for this audit. Existing
convergence and `common_tail_p0..p5` pieces are downstream mode/renderer
pieces and do not cover this retail branch.

## Classification and smallest slice

Class (a) on the proven natural lane: implement the fresh `C178` load and
the `C178 != 0` branch, advancing the natural cut to `0x8007185C`. Keep
the alternate call-bearing lanes at their first unresolved call frontier;
do not invoke a raw guest target or invent a helper result. The focused
certificate must cover `C178=1`, a canary `C178=0` stop at the first call
frontier, and a width/state mutant so that the natural advance cannot be
caused by a stale or wrongly typed load.

The following `0x8007185C` region is a separate audit and slice.
