#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/common"
container_run bash -lc 'test -x pc_port/build_native/xeno-port && timeout 10s xvfb-run -a pc_port/build_native/xeno-port >/tmp/xenogears-native-smoke.log 2>&1 || rc=$?; test "${rc:-0}" -eq 124 || { cat /tmp/xenogears-native-smoke.log; exit "${rc:-1}"; }'
