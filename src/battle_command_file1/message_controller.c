/* Retail archive (0x20, 0), file 1: controller 801E6CE8..801E71D4.
 * The PSX build compiles the byte-matched C reconstruction here. The native
 * battle runtime includes the same body with packed-RAM and helper bindings,
 * selected only after verifying the loaded archive-20/file-1 identity. */
#if !defined(SKIP_ASM)
#include "message_controller_impl.inc"
#endif
