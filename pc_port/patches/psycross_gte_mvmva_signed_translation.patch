diff --git a/src/gte/PsyX_GTE.cpp b/src/gte/PsyX_GTE.cpp
index a9d42f0..85e4e94 100644
--- a/src/gte/PsyX_GTE.cpp
+++ b/src/gte/PsyX_GTE.cpp
@@ -595,9 +595,11 @@ int GTE_operator(int op)
 			break;
 
 		default:
-			C2_MAC1 = A1(/*int44*/(long long)((long long)CV1(cv) << 12) + (MX11(mx) * VX(v)) + (MX12(mx) * VY(v)) + (MX13(mx) * VZ(v)));
-			C2_MAC2 = A2(/*int44*/(long long)((long long)CV2(cv) << 12) + (MX21(mx) * VX(v)) + (MX22(mx) * VY(v)) + (MX23(mx) * VZ(v)));
-			C2_MAC3 = A3(/*int44*/(long long)((long long)CV3(cv) << 12) + (MX31(mx) * VX(v)) + (MX32(mx) * VY(v)) + (MX33(mx) * VZ(v)));
+			// Signed 32-bit translation scaled to Q12 fits in 64 bits.
+			// Multiplication preserves the value without left-shifting negatives.
+			C2_MAC1 = A1((long long)CV1(cv) * 4096 + (MX11(mx) * VX(v)) + (MX12(mx) * VY(v)) + (MX13(mx) * VZ(v)));
+			C2_MAC2 = A2((long long)CV2(cv) * 4096 + (MX21(mx) * VX(v)) + (MX22(mx) * VY(v)) + (MX23(mx) * VZ(v)));
+			C2_MAC3 = A3((long long)CV3(cv) * 4096 + (MX31(mx) * VX(v)) + (MX32(mx) * VY(v)) + (MX33(mx) * VZ(v)));
 			break;
 		}
 
@@ -959,4 +961,4 @@ int GTE_operator(int op)
 	}
 
 	return 0;
-}
\ No newline at end of file
+}
