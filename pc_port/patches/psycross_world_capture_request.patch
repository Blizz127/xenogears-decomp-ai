diff --git a/src/PsyX_main.cpp b/src/PsyX_main.cpp
--- a/src/PsyX_main.cpp
+++ b/src/PsyX_main.cpp
@@ -850,0 +851,6 @@ void PsyX_TakeScreenshotPath(const char* path);
+/* _xeno_world_capture_request: optional port-owned capture request.  A weak
+ * hook keeps standalone PsyCross users independent of the world-map port. */
+#if defined(__GNUC__)
+extern "C" void PcPort_WorldCaptureFulfillAtPresent(void)
+	__attribute__((weak));
+#endif
@@ -867,0 +874,5 @@ void PsyX_EndScene()
+#if defined(__GNUC__)
+	if (PcPort_WorldCaptureFulfillAtPresent != NULL)
+		PcPort_WorldCaptureFulfillAtPresent();
+#endif
+
