# Sprite horizontal and vertical motion arithmetic

The native full production translation unit failed UBSan at a negative horizontal scaled shift. Retail SLUS80022B2C..80022D44 uses SLL, ADDU, NEGU and MULT/MFLO word semantics. The native owner now explicitly wraps horizontal displacement, vertical displacement and acceleration, floor shifts, and bounce multiplication. Signed predicates and rounding retain the original order. No movement constants, floor rules, callback targets or script paths changed.

The oracle executes the original536bytes/134instructions, including scaling80022CAC, with only battle floor query800BA8F4 replaced by a shared test fixture. The fixture records all256sprite bytes at query entry and writes a selected floor different from its initial value. Full output and callback count/state are compared against the actual native production TU. It does not validate the real floor-query implementation or active battle state.

276480cases per O0/O2/UBSan cover12boundary values each for position, velocity and gravity,5signed floors,4timers,4bounce coefficients, and both simple/floor paths. Horizontal inputs vary with the selected boundary indices. All133reachable instructions execute. The initial blanket134instruction assertion failed:80022C08 negates gravity inside the branch which already required gravity>0; there is no intervening writer in the tested single-threaded routine. The final assertion requires exactly this instruction absent and every other instruction present. No artificial state mutation was used to enter it. This coverage does not mean exhaustive arithmetic or scene parity.

Five controls reject a missing floor query, truncated bounce coefficient, omitted gravity, wrong horizontal velocity axis, and the original signed negative shift. Red/coverage/validated logs preserve the audit. Native build passes with73function stubs remaining. Neighboring scaling regression passes917504cases per O0/O2/UBSan plus its three controls; logs are included. Matching-tree temp1.c owners remain unchanged and require separate C-definedness/byte-match work. No live replay of this new binary yet; the earlier CompMatrix replay reached aftermathfield3 before a reboot. Full start-to-end Lahan retail1to1 remains open. No commit/push.

Matching-tree follow-up: ../lahan-sprite-matching-20260908 fixes temp1.c motion semantics and verifies byte identity for80022CAC(48bytes) and80022CDC(104bytes,3relocations). Vertical80022B2C remains396vs384bytes. Its bounded fixture passes, but full scene parity and native-override retirement remain open.

Ownership follow-up: ../lahan-motion-ownership-20260908 retires the three duplicate native motion owners after native-linked layout/arithmetic/mutant checks. The current build uses decompiled temp1.c; the already-running frozen replay predates retirement.
