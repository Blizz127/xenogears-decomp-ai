# Native object model relocation — 2026-09-08

Retail func801E742C tail801E7C40..801E7CDC moves its temporary high-end model copy into a new low-end allocation. Native construction previously stored the high copy and finalized it, omitting unrelocation, low allocation/copy, release of the temporary copy, and registry rebuild. The original movie4 heap snapshot has the retained11968-byte model at7F2A3C between free34884 and58784 regions. The omission is verified. The subsequent repaired natural run passed the allocation and completed movie4; see the live validation below for scope and pins.

OvlyFinalizeModelCopy now executes the retail sequence, including flags&2 setting object+A8 to zero, and mode-zero registry cleanup retaining packet allocations. Native pointer-width adaptation keeps the existing OvlyPtrTab representation. The corrected func8002C4BC is separately tested in ../lahan-model-unrelocate-20260908. Its exact match is stillFAIL236vs224.

The new test includes the full production overlay TU and calls the same private helper used by the constructor. It compares to raw retail tail instructions plus801DCE18 and801DC22C. Heap, model fixup/unfixup and memcpy are explicit oracle bridges; native heap/model calls have matching spies. This is boundary/order/data verification, not a complete constructor or real allocator test. It checks1152cases each atO0/O2/UBSan: flags0..7, counts0..8, eight copy patterns/sizes, null/non-null old registries. It compares the call sequence, model owner, copied bytes, count and every rebuilt model pointer. Seven negative controls rejected. Original sequence fails case1 on call order (red.log). Three retail code ranges are SHA-pinned by the runner; the disc-derived overlay remains in scratch outputs, not this evidence folder.

Existing cleanup regression passes39checks atO0/O2/UBSan and its controls on host. Container attempt stopped because Clang is absent; cleanup-host.log is the completed run. Native and full PSX builds pass. Native-only strlen declaration guard permits inclusion of the real heap API header alongside libc; PSX declaration is unchanged.

Fresh traced natural playthrough started in scratchpad/lahan-natural-20260908-model-relocation. Both previous live runs are preserved. No state patches, heap enlargement, skipped movie or manufactured checkpoint. No commit/push. Full Lahan retail1to1 goal remains active and unproven.

Opening runtime observation09:04: repaired constructor executed during the naturalopening and the game reachedfield14then13. Trace records low allocations plus releases of high temporary copies and old registry arrays (scratchpad/lahan-natural-20260908-model-relocation/opening-relocation-events.json). Example10664-byte allocation7D1FC4 precedes freeing temporary7F3A14. This demonstrates the live code path, not the later burning-Lahan movie4 acceptance or retail visual parity.

Live validation 10:57 CDT: the subsequent C5 repaired natural run completed STR4 through requested frame563 and returned to Citan dialogue. The88604-byte allocation now returned7ED270 (events60625/60626), passing the previous OOM point. movie4-live-validation.json pins the executable, trace excerpts and two screenshots. This validates the previously failing transition with the accumulated repairs; full constructor exactness and retail audiovisual parity remain unproven.
