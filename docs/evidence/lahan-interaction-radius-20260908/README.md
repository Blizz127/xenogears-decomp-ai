# Lahan interaction routine: retail branch repair

The natural New Game run `scratchpad/lahan-natural-20260908-atlasflip` reached field 17 via the mountain bridge, entered field 21, and could not activate its workshop exit from the walkable side. Player position near (123,0,-119), height64 and range16; door actor13 position (123,-58,-177), center offset (25,0,25), height38 and range16. Center distance is about41.4: outside inner40 but inside outer64. This captured geometry identified the radius mismatch; the final rebuilt run now has a separately observed successful exit, recorded below. No flags, positions, saves or geometry were forced.

All495 annotated words of `func_8008399C` (8008399C..80084158) were checked against the pinned retail field module. Retail uses the outer squared radius at80083F28 for normal confirm, and the inner squared radius at80084040 for passive interaction. The old C merged those gates under the inner radius. It also incorrectly made normal facing conditional on flag40000 and wrote D80285988 for normal radial interactions. That conditional facing rule and global write belong to the separate rectangular branch. The repair preserves that distinction, prior whole-map selections through the shared enqueue path, the skipped-parent byte, and the retail script-slot rules. Squared-distance addition wraps as retail ADDU does.

`bash pc_port/tests/run_field_interaction_retail_test.sh` compares a verbatim extracted production body with the complete retail routine executed by the MIPS interpreter. The pre-repair body fails the first annulus case (before.log). Tests compare the whole actor/sprite fixture, relevant output globals, and ratan2/rectangle/script-lookup call traces. Controlled helper boundaries are explicit: this does not prove those helpers' implementations, arbitrary helper side effects/aliasing, or complete GTE state. SQR uses signed16 inputs and32-bit products. Every retail instruction is exercised; instruction coverage does not mean every possible state combination is covered.

Coverage includes inner/outer boundaries; whole-map, rectangular and radial flags; facing sweep; signed helper angles; locks/system modes; multiple actors; busy and already-running slots; parent skip; height rejection; every16-bit player radius with both fixed and complementary target radii; and signed16 coordinate extremes. Seven negative controls must fail. `tests.log` records current totals. Native and full PSX builds pass (build logs).

Exact decompilation remains **FAIL**: generated function size1732 versus retail1980. `check-compiled-size.py` reports this necessary gate; no full relocation-normalized byte match is claimed. Whole-game retail audiovisual parity, the complete Lahan story, and human acceptance remain unproven.

Fresh natural run `scratchpad/lahan-natural-20260908-interaction` uses frozen SHA256 `9c268be158fd77cb8d417ed03533de7cba7679fed65a8705767091c1d506abfc`. It includes the branch repair before the subsequent defined-wrap expression change; it is not the later rebuilt executable. Its runtime results must retain that distinction. The final driver log supersedes the initial before-field14 estimate: it reached field14 at03:43:37 and terminated at03:43:56. Previous frozen run remains separate and unchanged. No commit or push.

The intermediate fresh run was stopped after the driver had reached field14; no manual route input followed after native byte comparison showed the defined-wrap change altered the function code. The final runtime run is `scratchpad/lahan-natural-20260908-interaction-wrap`, SHA256 `4ca98617db4aa64becd2084657d88fee5beffcca5a55ec29499c87cc57bd51d0`. The earlier executable was not hot-patched. The final run subsequently reached field17 naturally via the western gap and rope bridge, entered field21, and exited through ordinary direction/confirm input. `workshop-before-exit.log` records field21 at (114,0,-115); `workshop-exit-face` held Down+Right for0.08 seconds, then `workshop-exit-confirm` pressed Z for0.12 seconds. `workshop-after-exit.log` records field17 at (-446,31,448), with corresponding screenshots and manual-input.log. This is native runtime doorway proof, not a captured instruction trace or retail audiovisual comparison. No positions, flags, saves, geometry or executable state were forced.
