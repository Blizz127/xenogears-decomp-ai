# Camera initialization after Alice's scene

The unchanged `lahan-natural-20260908-interaction-wrap` run naturally completed Alice's field12 scene, advanced scenario7 to8, and returned to field11. The resulting camera remained inside geometry while ordinary movement still moved Fei. Its read-only capture `alice-return-camera-state.log` records camera mode1, zero eye/eye2, target(-7536640,0,17956864), interpolation(1,1,259,259), and zero movement flags/timers. Screenshots, input log, and RAM dump are preserved in that run. No game-state values were changed.

The retail initializer `func_8007254C`, 8007254C..800726E8, explicitly resets camera mode with SH at800726C4 to800AF934. The former C body omitted that store, the flags/timer resets immediately following it, three preceding scene halfword resets, and the matrix helper call at80072638. The repair restores those operations using named globals and the existing scene matrix member. It does not impose a new camera policy. All103 annotated instruction words were checked against the pinned retail field.bin. See provenance.json.

`bash pc_port/tests/run_field_camera_init_retail_test.sh` executes the complete103-instruction retail routine and compares the extracted production body over a0x500-byte state region, including untouched bytes and matrix-call count. The4096 initial patterns include all uniform byte values and deterministic random states. O0/O2/UBSan pass; eight omission controls fail. The pre-repair body fails on the missing matrix call. The matrix helper is an explicit controlled boundary that writes identity rotation/zero translation while preserving its padding; this test does not prove RotMatrix or GTE behavior or all store ordering.

Native build and full PSX build pass. Exact decompilation is **FAIL**: current compiled size520 versus retail412. The size gate is necessary only; no relocation-normalized match is claimed. The current body remains a behavioral repair requiring further exact-match work.

Fresh natural runtime verification is **PASS for the bounded camera return** in `scratchpad/lahan-natural-20260908-camera-init`, frozen SHA256652e2e73385205f1ff9654a293d69f8b4488bf5d813403f9aaa87d51fced554c. It starts at New Game with ordinary input and a four-hour observation deadline. The failure-reference process remains separate and was not hot-patched. No commit or push. Whole Lahan and retail audiovisual parity remain unproven.

The fresh run completed Alice's final monologue using ordinary confirm input. `alice-return-camera-state.log` records field11, scenario8, player(-115,0,274), mode0, eye/eye2(-50582784,-30474240,61003008), and interpolation(8,8,-1536,-1536). `shots/alice-epilogue2.png` shows the room and Fei normally. This is the same player position and story state as the failure reference, whose mode remained1 and eye zero. `alice-camera-acceptance.json` preserves the executable pin and evidence paths. No game-state writes were used. Exact matching remains FAIL520vs412; retail audiovisual and whole-Lahan acceptance remain unproven.
