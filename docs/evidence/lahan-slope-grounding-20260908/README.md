# Retail slope grounding correction

Natural run445617 stopped at mountain15 position(-863,-256,-138), triangle1623, material00400000, after the third battle. The menu and encounter processing remained responsive. Read-only RAM capture and ordinary input screenshots are retained under scratchpad/lahan-natural-20260908-party-remove. The fourth battle was also completed before diagnosis; Fei has28/58HP,25EXP,level2,two earned Hob-Jerkys and100gold.

Retail80085104-80085144 conditionally clears ActorData+F0 only when material420000 is absent, then unconditionally settles vertical velocity, flags and position. The previous native body incorrectly skipped settling on flagged slopes and omitted the conditional accumulator clear. The correction mirrors that control flow. The original captured-state native output differed from retail only in actor Y, sprite Y and sprite vertical velocity after the compared sequence.

The diagnostic runner executes original retail82620,82BB8,84A40 via the MIPS adapter and compares against the compiled production owners. It uses captured actor/sprite/walkmesh state, with96 explicit synthetic material/accumulator/gravity variants. Full compared actor and sprite buffers match with the correction at O0 and O2; the old owner fails. The history-recording81C54 callback is excluded in both. Native unreachable callbacks abort. The comparison uses the same PsyCross GTE implementation with an integer NCLIP callback on the interpreter side; it is not a hardware oracle or exhaustive routine proof. Candidate84A40 arguments are selectedY=7fffffff,targetState0; they were not captured at live function entry. The runner rebuilds the current misc8.c and misc4.c owners before linking; GTE and interpreter objects come from the existing battle_gte_retail_test build. Reproduction: bash scratchpad/lahan-natural-20260908-party-remove/run-slope-ground.sh then python3 scratchpad/lahan-natural-20260908-party-remove/check-slope-ground.py. Objects and captured payloads remain local and are not committed.

Native and full PSX builds pass. The complete function remains2396compiled versus2704retail bytes, so exact decompilation is FAIL. The original stuck run predates this correction. A fresh corrected build has now recovered from the slope and traversed the mountain to field17; see [the runtime observation](runtime-observation.md) for evidence and limits. Party-removal departure verification and complete Lahan retail parity remain PENDING. No commit or push.
