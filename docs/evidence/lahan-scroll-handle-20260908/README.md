# Scrollbar handle submission

The fresh items-scrollbar run naturally opened Items fromfield14 and crashed in func_801D1464, after scrollbar construction. Core pData=0x7002a000000000 came from raw native g_Menu+43C. The producer func_801D3344 already allocates/stores the named pScrollHandle and its named vertices/context. The consumer now uses those same members and the named scrollHandleActive byte, without adding a guard or altering the retail branch.

Retail801D1464..801D14B0 contains19instructions/76bytes. The regression verifies all annotated words against the pinned menu.bin and extracts the actual production body with real headers. All65536 flag/context combinations pass O0/O2/UBSan; inactive cases supply a null handle and must not draw. Four controls reject restricting the active flag to1, wrongcontext, wrongvertices, and wrongprimitivecount. Helpers are explicit boundaries; no GPU/helper internals are proved. The old body fails at runtime with SIGSEGV in the regression after the test helper signature was corrected to accept both old and new pointer types.

Native/fullPSX builds pass,73nativefunctionstubs. compare-object.py resolves three zero-addend relocations to retail addresses and compares all76bytes: exact function matchPASS. This does not prove whole-ROM or visual parity. The scrollbar builder from the previous turn still fails exact matching (352vs356); object-comparison.json there now records resolved instruction differences rather than only a size gate.

Fresh natural acceptance isPENDING in scratchpad/lahan-natural-20260908-scroll-handle. Frozen SHAe403f542b9c1d853a8a149cead4f35f2d44657d3e81e552b586669b7a203d557, driver4299. Previous core and input logs remain preserved. No forcedstate/commit/push. FullLahan goal remainsactive.

Natural run now proves bounded empty-inventory Items entry and exit: visible windows/scrollbar, C back to main menu, C back tofield14, Left movesFei. See items-acceptance.json and screenshots in scroll-handle run. Read-only typed GameState capture shows zero item IDs/quantities; item use NOT_RUN. A changed/striped wall-painting texture after menu return is observed and unresolved; this prevents a visual-parity claim. The initial direct g_GameState field read failed due debug symbol type, then explicit GameState cast succeeded; no state writes.

Subsequent natural run earnedHob-Jerky1 in battle, opened populatedItems, observed itsdescription andtargetprompt. AtfullHP itremainedquantity1. Aftera secondbattleleftFei46/50, ordinaryuse healed50/50, depleteditemID57/quantity1to0/0, andreturnedthroughmainmenutofield15 withoutcrash. hob-jerky-acceptance.json locatesallcaptures. This is bounded one-item lifecycle proof, not allItems orretailAVparity.

## Subsequent ordinary-input observation

The same frozen executable subsequently passed a bounded populated-inventory Items lifecycle. Battle 1 awarded one Hob-Jerky (item 57). A full-HP use attempt retained quantity 1. After battle 2 reduced Fei to 46/50 HP, ordinary menu confirmation healed him to 50/50, cleared the consumed item ID/quantity, and returned safely to the field. See `scratchpad/lahan-natural-20260908-scroll-handle/hob-jerky-acceptance.json` and its referenced screenshots/read-only inventory captures. This supersedes the earlier item-use NOT_RUN status for this one item and path only. The wall-painting texture discrepancy, other item behavior, and retail audiovisual parity remain unresolved.
