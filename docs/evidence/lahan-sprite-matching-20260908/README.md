# Matching-tree sprite motion

The matching-tree temp1.c vertical owner omitted retail80022B88's early return when the current signed Y halfword equals the floor, and declared/called800BA8F4 without the sprite argument. Its original production TU fails the shared retail-instruction fixture atcase2145 (y0,velocity1,gravity1,floor0,timer0), changing sprite bytes that retail leaves unchanged. The floor query is a controlled callback fixture; real battle-floor behavior is not proved here.

The matching-tree vertical owner now follows the verified native order with explicit word-wrap arithmetic and the correct argument. The horizontal owner uses unsigned shifts/additions without changing its generated retail instructions. The scaling owner uses a wide signed product narrowed to32bits and the retail conditional structure, avoiding host signed multiplication overflow while generating the original MULT/MFLO.

Full PSX build passes. The byte verifier pins the retail SLUS SHA, reads the actual compiled ELF function, resolves only validated R_MIPS_26 calls, and compares every byte:80022CAC MATCH48bytes/no relocations;80022CDC MATCH104bytes/3relocations. Changed-instruction and wrong-target controls reject. This is per-function identity at retail placement, not whole executable identity.80022B2C remains NONMATCHING396bytes versus384retail; no completion claim for it.

The full matching production TU passes276480motion and917504scale comparisons each at O0/O2/UBSan. The motion fixture covers133/134instructions, with the gravity-negation branch exclusion explained in ../lahan-sprite-motion-20260908. Controls reject missing floor equality, invalid floor argument, and signed negative shift. The native motion regression also passes with its fivecontrols. Existing native ownership overrides remain in place; retirement is separate work. Native build passes with73function stubs remaining; see native-build.log. No live replay of these changes; full Lahan1to1, actual floor integration, and remaining matching functions stay open. No commit/push.

Further vertical exact work: isolated candidates in scratchpad/lahan-vertical-exact-20260908 remain nonmatching. One volatile-store experiment reaches384bytes with8differentwords; it was NOT installed because byte proximity does not justify changing memory qualification. The tested production owner remains396bytes. Candidate results are preserved in vertical-candidates.json. Fresh standard native replay is scratchpad/lahan-natural-20260908-motion-matching, without the older allocator wrappers.

Ownership follow-up: ../lahan-motion-ownership-20260908 retires the three duplicate native motion owners after native-linked layout/arithmetic/mutant checks. The current build uses decompiled temp1.c; the already-running frozen replay predates retirement.
