# Field exact-match audit and restored axis-to-matrix function

Status: missing `func_800759E4` reconstructed as C and independently verified exact in both standalone and actual full `misc2.c` translation-unit compiles. Parent integrated the unchanged insertion. Dedicated actual native production-source differential is GREEN. **Whole field module remains nonmatching**; this is a 292-byte missing implementation repair, not module or gameplay acceptance.

## Whole-field diagnosis before this insertion

Pinned retail `disc/field.bin` SHA256 `38a1ce829a6f094c505f67143d6ace2d328418c65425a7383991179467e1fdfc`, size 260862. Parent/private generated baseline was 242330 bytes, net -18532 / `-0x4864`.

| Section | Retail bytes | Generated bytes | Delta |
|---|---:|---:|---:|
| rodata | 0x2FC | 0x41C | +0x120 |
| text | 0x3DC7C | 0x392F8 | -0x4984 |
| data | 0x1B84 | 0x1B84 | 0 |

The first raw mismatch `0x1B8` is `main/misc11` jump-table relocation, not missing table content: its 0x74-byte block length and zero words match, and every nonzero pointer is shifted by -0x328C (e.g. 8009483C to 800915B0). Inserting literal retail pointers would conceal shifted code addresses. Extra emitted C rodata comes from misc4 (0x7C), misc8 (0x68), misc (0x20), misc5 (0x1C); original literal tables also remain in the rodata assembly.

`inventory.py`, `object-inventory.json`, and `function-size-inventory.json` account for the entire -0x4984 text delta. Baseline shared field source matched the private copy. Largest object deficits are misc2 (-4392), text_box_render (-3988), virtual_machine (-3784), misc8 (-2012), misc4 (-1764), misc (-1156). These include helper extraction/layout and abbreviated C, not a single omitted tail. Examples: func8007E1C0 3148→1144, func800A3474 2072→416 with extracted copy helper, func800A3F4C 2044→524 with extracted copy helper, and func80075B44 2416→1140 with asserted unimplemented branches in the matching configuration.

The earliest text mismatch is structural: an emitted 100-byte `FieldLoadLightRecord` helper precedes retail entry func8006FDEC. Its current C body is 496 bytes vs retail672; whole generated text also begins +0x120 later due extra rodata. A later synthetic `IsLiveHeapBlock` contributes another132 bytes. Lighting experiments are retained under this directory but remain nonmatching; some earliest experiment links rounded the symbol up4 bytes before the runner gained an explicit output address and symbol assertion. None is an accepted candidate. Parent explicitly prioritized the genuinely absent function below.

## Retail authority and candidate

`func_800759E4` occupies retail `800759E4..80075B08`, raw `0x5EF4..0x6018`, exactly292 bytes/73 instructions, SHA256 `b4f76d0b3dd362838d4eb82c2dfefab068628db24f11de43f7fe0504c8b04cf3`. Consolidated retail disassembly is private `asm/field/main/misc2.s:3889`. Before this insertion the retail symbol had no matching source/header definition; this was not a rename of `FieldActorSyncSpritePosition`.

The function accepts `(MATRIX *matrix, VECTOR *axis)`, allocates0x58 bytes stack, copies the full16-byte seed vector at8006FB70, and calls:

1. `OuterProduct12(up, axis, temporary)` at resident8004A480.
2. `VectorNormal(temporary, right)` at80048D7C; return ignored.
3. `OuterProduct12(right, axis, temporary)`.
4. `VectorNormal(temporary, up)`; return ignored.

It stores right xyz into matrix row0, freshly loads axis xyz for row1, then up xyz for row2. Nine halfword stores leave two bytes of matrix padding and all three translation words untouched. No new heap allocation or gameplay branches are introduced.

Seed authority: retail field raw0x80..0x90 /8006FB70 is `00 00 00 00 00 00 00 00 00 10 00 00 00 00 00 00`, one VECTOR `{0,0,4096,0}`. The insertion declares the existing retail rodata symbol on PSX and defines a typed `const VECTOR` only under XENO_PC_PORT. This prevents a generated native zero placeholder from supplying the wrong seed. Existing actual PSYQ/PsyCross headers define the function interfaces; native VECTOR16, MATRIX32 and translation offset20 are asserted by the fixture.

Source-before SHA `8a4368075cc1a48706e52acb50781b3c2513d717eab5778481ade01a2279ed96`.
Insertion `missing-759e4/insertion.inc` SHA `728d867ae2214f0c3c85d8789d4bf20f02b00f06afa7eaef27172aa58f11518c`.
Full scratch/installed candidate SHA `c2539494ca3d29971c4af8b2815a5c61ca10452839ef05b338edcec3ed3ecaea`.
Removing the insertion exactly recovers source-before; no surrounding edits in this lane.

## Exact compiler verification

Standalone minimal-type C and the full scratch production TU both emit the exact292 retail bytes. Full-TU result is `missing-759e4/full-comparison.json`; symbol checked at800759E4 and extracted text SHA matches retail. Full-TU text is17532 bytes vs baseline17240, precisely +292.

Compiler is repository GCC2.7.2 PSX cc1, actual private field settings `-O2 -G8 -mips1 -mcpu=3000 -funsigned-char -fpeephole -ffunction-cse -fpcc-struct-return -fcommon -fverbose-asm -msoft-float -mgas -fgnu-linker`, with repository MASPSX and MIPS assembler. Commands retained in full_compile.py/full-comparison.json. PSX preprocessing uses actual shared include headers. Symbol placement uses explicit `.text ADDRESS : SUBALIGN(4)`.

**Isolated link limitation:** the whole TU contains unrelated unresolved references. The three dependencies used by this292-byte function are pinned to exact retail addresses; other externs are assigned a valid unused KSEG address solely so unrelated JAL relocations can link. Those placeholders are not a full-TU correctness claim. No assembly instruction or output byte was patched. Whole-module relink/offset accounting after integration is parent-owned.

Expected net size after this insertion alone is242622, leaving -18240 / -0x4740 vs retail. The extra rodata and earlier lighting layout mismatch remain; the first table pointer mismatch will shift by292, not disappear. Exact full module still requires repairing cumulative C/layout discrepancies.

## Native differential and negative controls

Durable files:

- `pc_port/tests/field_axis_matrix_retail_test.c`, SHA `1e2a64ec19f1add95a7fbb22323402c484fd5918b77fcd67e5aab2c0f218f3d1`.
- `pc_port/tests/run_field_axis_matrix_retail_test.sh`, SHA `39b0e2203922771dc5503cb5a27c287ce5ea1fdce32eed2ca48093141e85efd5`.

Default runner compiles actual full production misc2.c and actual production seed. Raw instruction oracle uses production battle_mips_adapter and freshly reads the pinned retail disc slice/seed. It checks the complete disc SHA, function slice SHA, seed bytes and source/header/runner hashes before execution, then rechecks source pins afterward. Overrides `FIELD_AXIS_MATRIX_SOURCE` and `FIELD_AXIS_MATRIX_OUT` support independent source/control runs; no scratch file is mandatory.

Retained installed-source run: `missing-759e4/durable-output`. O0/O2/all-Clang UBSan each **PASS200 cases,21078 checks, all73 retail instruction slots**. Cases include zero, Q12 unit, halfword sign boundaries, full-word INT_MIN/INT_MAX/-1/high bits, nonzero vector pads, two retained matrix/redzone byte patterns, and deliberate axis mutation after every helper call to test fresh input/reload ordering. Tests compare four ordered helper events, complete input vectors, pointer roles/alias relationships among the expected locals, resulting matrix+32 guard bytes, and post-helper axis content. Padding and all translation bytes must retain the initial pattern.

Four compiled O2 semantic controls are rejected only if exit status is exactly1 and log contains `FIELD AXIS FAIL`: wrong seed, wrong axis component, reversed second cross-product arguments, omitted final normalization. Baseline reconstructed source-before fails linking specifically for absent func800759E4 and D8006FB70 (`baseline-absence.log`), classified as absence proof, not a semantic differential run.

Clang full TU initially rejected four unrelated definitions after earlier implicit declarations. Runner generates four correct test-only forward declarations (FieldMatrixCopy/Translation/Transform, func80281B00), retaining actual production function bodies and actual SDK helper declarations. This enables **all-Clang** UBSan compilation and linking without mixed instrumentation. Initial compiler diagnostics retained in native-clang-build.log. No shared compatibility header was changed.

Limits: OuterProduct12 and VectorNormal are explicit deterministic boundary spies, not SDK arithmetic or GTE emulation; the test establishes their call ordering, arguments, output use, fresh reads and all matrix stores. It does not establish rendering, natural gameplay invocation, invalid-pointer semantics, or overlapping MATRIX/VECTOR objects. The asserted rotated actor branch remains unchanged. Native runtime/build, whole-module integration, and visual acceptance belong to parent/other agents. All shared edits from this lane are the two authorized test files; parent installed production insertion independently.

## Parent integration gate (reported independently)

Parent independently reran installed durable suite at `/tmp/xeno-field-axis-matrix.w8zti0wd`: all three modes PASS200/21078/73, four controls rejected. Parent verified function292 bytes plus seed16 bytes directly against retail, then rebuilt the private global target: field length is242622 (+292), three existing checksum failures remain, and member-change full module remains exact. Parent native binary SHA prefix `cda8623` links the real psyq_compat math dependencies (not fixture stubs). Parent durable handoff: `docs/evidence/field-axis-matrix-20260906`. These integration results are parent-reported; no extra native build or runtime was launched in this lane.
