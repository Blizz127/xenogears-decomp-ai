# Equip core control-flow transcription

The original2472-byte routine801E05D0..801E0F78 has been transcribed into the native menu owner. It implements equipment-slot and item-list input states, character navigation, conditional group switching, window construction, cursor vertices, preview calls, scroll limits, repeat-confirm behavior, and exit callbacks. It replaces the native core stub. The PSX build still consumes the original INCLUDE_ASM; no new exact C match is claimed.

The differential runner executes original menu.bin instructions and the verbatim complete production core body. Both use the same deterministic23callee fixtures, which record all arguments and model input/transition timing and selected return values.400scripted cases include random command sequences and targeted up/down scrolling, five list sizes0/1/7/20/100, both render contexts, both equipment modes, conditional character4behavior, navigation change/nochange, confirmation success/failure and opening transition waits. All618retail instructions are exercised in aggregate. O0/O2/UBSanPASS; fullmanager,cursors,workbuffer,input/transitionstate andorderedcalls agree. Six separately compiled negative controls reject cursorX,scrollclamp,groupgate,navigationdirection,preview omission andexitcleanup changes. Instruction coverage is not exhaustive path/input coverage. Run bash pc_port/tests/run_menu_equip_core_test.sh.

Native/fullPSXbuildsPASS. Native stubcount changes73->79 because one core stub is removed and seven previously unreferenced callees become linked:801DE5CC,801DF0D4,801DF5D0,801DF890,801DFB68,801DFF5C,801E36D4. The earlier census only observed that these names were absent from stubs.c; it did not establish implementations. The new linked dependency report is dependencies.json.

Runtime Equip remains UNVERIFIED and not ready for acceptance. Those seven helpers need retail-backed implementations. Initialization/teardown801DE2C8/801DE36C also use raw widened pointers at menu+0x434 and request unported resource modes7/0x17; these require audit. Other callee semantics and GPU output are not proven by callback fixtures. Do not launch a fresh acceptance run as if these dependencies were complete. The original natural run1300347 remains frozen in its previous error loop, unmodified. Menu textureYfix still awaits fresh runtime validation. Full Lahan100percentretail goal remainsACTIVE; no commit/push.
