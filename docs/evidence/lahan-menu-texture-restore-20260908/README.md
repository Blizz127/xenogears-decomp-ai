# Menu return texture-strip destination correction

Retail 8007A1D8..8007A21C restores two64x256 texture strips at(704,256) and(960,256). The native func_800799D4 restored both atY0. The retail delay-slot SH at8007A1FC explicitly writesY256. The68-byte instruction region is verified against disc/field.bin, SHA2564fe9eee5e7f887b2c5361c5b372b281ff342550406a0e7634887de66bc64dccc. Only the native restoreY was changed, with a source comment. The full PSX wrapper still uses its original INCLUDE_ASM; this is not a new exact-match decompilation claim.

The regression executes those original MIPS instructions with recording/software-VRAM LoadImage and DrawSync bridges, then compares the entire1MiB VRAM against the extracted verbatim production restoration statements. The two pixel buffers and existing VRAM use32distinct coordinate-dependent patterns. Old code fails; corrected code passes atO0/O2/UBSan. Four separately compiled negative controls reject Y0,wrongX,wrongsource,andmissingsecondload. Run bash pc_port/tests/run_field_menu_texture_restore_test.sh. This proves the bounded destination/payload effects with shared software image-copy semantics, not hardware GPU behavior or the complete menu lifecycle.

Native and fullPSXbuildsPASS. No commit/push. The fresh natural lighting run1300347 is frozen on the pre-fix binarya1196822. It reached painting14 and opened/closed the root menu normally. Read-only VRAM snapshots before and after show changes in both relevant lower strips; see old-runtime-vram.json. The current side-on camera view did not visibly reproduce the striped painting, so attribution of that visual symptom remainsunproven. A fresh corrected-build menu return is pending.

During the next ordinary menu attempt, the first Down arrived during menu opening, selectingEquip instead ofItems. Equip then hit GameHandleError131 via HeapFree(NULL). Read-only backtrace: func_801D4EA0(window2) -> func_801DE36C -> func_801E3088 -> rootmenu. Log reports resource mode3and19unported and stubfunc_801E05D0. No recovery flags, live patches or error bypass were used. The blocked natural run remains available for diagnosis. This is an additional genuine fidelity gap, not evidence the texture correction failed. See scratchpad/lahan-natural-20260908-light-init/equip-stall-backtrace.txt andrun.log.
