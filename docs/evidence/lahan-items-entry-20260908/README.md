# Natural Items-entry crash

The frozen camera-init run naturally completed Alice's scene, reached scenario8 in field15, and won three random battles. After the third reward screen, ordinary V opened the main menu; Down, Down, Z selected Items. The process1887792 then terminated with SIGSEGV at06:02:30CDT on2026-09-08, before the driver deadline. Driver log records end rc=-11 at06:02:31. The subsequent confirm attempt failed because the display had already been cleaned up; it did not send input.

Evidence is under `scratchpad/lahan-natural-20260908-camera-init`: frozen executable, core, core-export.log, menu-crash-gdb.log, manual-input.log, and shots/menu-items-open.png. No game-state writes or injected checkpoints were used. Last visible menu state was Fei level2 HP22/53 EP10/11. Ledge NPC jumping dialogue did not advance scenario8; the earlier Dan name was not verified. Two gap attempts were interrupted by encounters; a field-position read during battle3 transition is not valid landing evidence.

Core stack: func_801C72BC(mode0) at misc.c1042, called from func_801DA4A8, func_801DBE54, and the normal main-menu dispatcher. Work is null. The caller allocated work0x6FD6E4 and wrote it at native g_Menu+0x42C. The named g_Menu->unk42C slot is actually native offset0x530 and remains zero. Retail initializer801DA4A8..801DA518 stores a four-byte pointer at retail offset0x42C. The native raw void-pointer store is both at the wrong native member and eight bytes wide.

Two additional initializer layout discrepancies are directly visible: native ItemMenuWork size4784 (0x12B0), but allocation/clear uses retail4504 (0x1198); native itemMenuStrings offset0x1290, but setup passes raw0x10E0. Native descriptionBundle offset0x1298 is beyond that allocation. A null guard would not repair these relationships.

The initializer repair is implemented using named members and native sizeof. Its production-body regression uses the real production structures, tests all256 byte patterns with stable and helper-replaced g_Menu contexts (512 cases per build), checks call order, pointer publication before clearing, full clear, and untouched menu bytes. The old body fails the string-address check; four independently compiled controls reject the wrong slot, wrong string address, undersized allocation, and undersized clear. O0/O2/UBSan pass. Helpers are controlled boundaries; this does not prove CD, text, allocator behavior, or live Items entry. Check teardown separately against the retail call sequence; do not simplify apparent repeated frees without tracing the heap semantics. Both initializer and teardown annotated instruction words were verified against disc/menu.bin; provenance.json contains pins. This is diagnosis, not exact-match or whole-Lahan completion. No commit or push.

Native and full PSX builds pass; native link retains73 function stubs. The initializer object is112bytes, matching retail size. `check-initializer-match.py` resolves all11 zero-addend R_MIPS_26/HI16/LO16 relocations to the retail addresses and compares all112 bytes: exact function match PASS, no differing words. This is not a whole-ROM match. `exact-match.json` pins the object. `tests.log` identifies build_native/items-init.G3RNcaAx, whose pins.json records the tested source/header/harness/runner. No runtime acceptance is claimed yet; the earlier run remains terminal. Next inspect/repair teardown's raw native offsets with its own retail call-sequence test, then run Items entry/use/exit naturally.

Teardown `func_801DA518` now accesses named native members and the packed four-byte description slot, preserving all seven helper calls and reloading g_Menu across them. Its extracted production-body test passes512 cases each O0/O2/UBSan, including helper-replaced globals and byte canaries. Six controls reject wrong description offset, native-width description read, missing work release, wrong window, missing disable byte, and cached global. The previous raw-offset body crashes the regression. This checks boundary call order/arguments and structure effects, not helper internals. `exit-tests.log` identifies items-exit.9uF7CNMX. Native/fullPSX builds pass. `check-teardown-match.py` resolves15 zero-addend relocations; all164 bytes match pinned retail, no differences. Initializer match was refreshed against the final object.

Fresh natural runtime verification is PENDING in `scratchpad/lahan-natural-20260908-items-layout`, frozen SHAa26bd55f0c4bb49584d938ea2dd326ccd266833f21b328c9f7acbf945bdca7c6. The terminated camera-init run remains preserved. The current native HeapFree marks block metadata rather than releasing host memory directly; no repeated retail call was removed. Full allocator fidelity is outside these layout tests.

Fresh runtime result: FAIL later in Items window creation. The field14 run selected Items through normal menu input and terminated SIGSEGV (driver06:19:42). Core stack reaches func_8002675C with tableNULL from func_801D3C4C, after initializer and item-page work. The latter still uses raw menu offsets0x364/0x2DC/0x308 and raw window offsets. Caller argument mapping also needs retail inspection: core has window width292,height132 while func_801D3C4C fourth parameter is292; no conclusion about correctness yet. The new run core and crash-gdb.log are preserved. Neither Items entry nor use/exit has passed natural runtime verification. The two function-level exact matches remain bounded evidence.
