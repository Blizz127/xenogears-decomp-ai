# Lahan destruction CompMatrix boundary

The camera-state natural New Game run reached the third scripted battle segment in burning Lahan and terminated with SIGSEGV after Fei's “Stop it!” dialogue. Its frozen binary SHA is in before-pins.json; the core and RAM remain in scratchpad/lahan-natural-20260908-camera-state. The captured retail callback800AFC68 calls CompMatrix at800AFCBC with a0=0xC,a1=0x1408,a2=0x1F800000. The generic native bridge treated the first two addresses as host pointers.

Executing the original352-byte CompMatrix routine8004931C..80049478 on the captured low RAM returns normally after88instructions with a zero output matrix. This is a bounded instruction/GTE experiment, not a retail console replay. The callback extension refers to actor80162E18, whose first two words are zero in the core. Its earlier lifetime and whether that state matches retail remain unresolved; the repair does not change that reference, clear tasks, skip the callback, or alter scene progression.

The typed CompMatrix bridge now resolves all three matrix pointers, including physical RAM zero and low addresses, through the existing guest/native address map. It returns the original guest destination. A separate SDK mismatch was exposed by the oracle: retail SWC2 IR3 at800493F8 writes a complete sign-extended word at matrix+0x10; PsyCross left bytes+0x12/+0x13 untouched. The tracked word-store patch restores those two bytes and is applied by build_port.sh after the existing alias patch. Applying it to the captured pre-edit vendor source produces exactly the current vendor file.

Validation:5120 comparisons each at O0,O2,UBSan, plus the captured address pattern. Five pointer domains, four exact-alias modes, full32-byte matrix output, surrounding bytes and guest return address are compared against original SLUS instructions. Both sides share PsyCross's GTE backend; this does not establish hardware-level GTE accuracy. Translation samples are signed16-bit values; partial matrix overlaps and all possible arithmetic inputs are not exhaustive. Four controls reject removed typed dispatch, missing physical aliasing, a host return pointer, and omitted word store. The original dispatch control segfaults as expected. The broader GTE suite passes after supplying its missing test-only g_PcPortPresentedFrames definition; the initial link failure is retained separately. Native build passes with73function stubs remaining. No matching PSX TU changed.

Fresh replay: scratchpad/lahan-natural-20260908-compmatrix, binary60a0d9ee822b9288e479f128fc2e77305f29444a68819db56edbe7780c6826c2. Ordinary New Game inputs only, external allocator logging wrappers as in the previous run. Destruction-scene runtime clearance observed in this frozen replay: after the same Stop it dialogue, the process remained live and reached the aftermath Uh... Uhn...? text. See live-validation.json and live screenshots. This does not resolve actor lifetime or establish retail audiovisual parity. Full start-to-end Lahan retail1to1 remains unproven. No commit or push.
