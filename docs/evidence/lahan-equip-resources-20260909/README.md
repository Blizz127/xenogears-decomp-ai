# Equip resource allocation/free paths

The natural field14 run1300347 hit GameHandleError131 after ordinary Equip entry. The root log records unported resource modes3/19 and native stub801E05D0. The resource implementation now follows retail mode3 and0x13: decompress archive entries2,3,0x2B,0x14 into resource offsets0,4,0x18,0x14; free those four pointers in the same order. Native pointer members use their widened struct fields, while unnamed slots in unk8 remain32-bit. No HeapFree(NULL) guard, error bypass, forced exit, or live patch was introduced.

The complete2128-byte resource dispatcher801C72BC..801C7B0C is verified against disc/menu.bin; SHA2562f315236e4074f75cf1121fc8e865958635fabf229c3229552d49636857971c1. The test executes this retail routine for both Equip modes through the existing MIPS interpreter, including its archive lifecycle. Allocation/decompression/free callbacks record ordered arguments and return distinct pointer tokens. Native production misc.c is linked in full with unused sections discarded.512cases vary64initial resource patterns and4upper-byte mode patterns for both paths. Complete native-layout resource contents and all callback events agree atO0/O2/UBSan. Four negative controls reject wrong weapon entry, wrong extra bank, wrong description slot and reversed first frees. The pre-change native body fails for mode3 with8events versus12retail. Run bash pc_port/tests/run_menu_equip_resources_test.sh.

Limits: callbacks are shared deterministic fixtures, not actual decompression/heap internals; their global owner pointers remain stable. Other dispatcher modes are not covered by this test. The PSX build retains the existing assembly owner; this change is not a complete dispatcher decompilation or an exact new C match. Native/fullPSXbuildsPASS. No commit/push.

Equip remains INCOMPLETE. Its core801E05D0 is still a native stub, and the frozen natural run remains in the preserved error loop. A retail-byte audit identifies the full core as2472bytes with23direct callees, none listed as native stubs in the current build. This does not prove those callees are complete or correct. See equip-core-audit.json. Next: transcribe and differentially validate the full core control flow and audit its callees before starting a fresh natural Equip/menu-return run. Full Lahan100percentretail accuracy remains ACTIVE and unproven.
