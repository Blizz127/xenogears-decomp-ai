# Retail atlas builder800263E4

Two normal field15 battles reached a generated native stub for800263E4 while opening menus. CD run1019197 and AB run1297561 terminated; the separate atlasflip run includes this repair. The captured CD call has table006FB18C, index64(hex), packets800D5D14, buffer0, x32,y204,scale4096,flipX0,flipY1. The captured entry has one quad. The crash core and table dump remain under scratchpad/lahan-natural-20260908-cd; they are not copied into this evidence directory.

The decompiled function follows the actual888-byte retail routine800263E4..8002675C: signed geometry scaled by an unsigned16-bit factor with truncation toward zero; nine arguments with independent low-byte global flips; local descriptor flips; alternating40-byte packet slots at80-byte item strides; and UV correction based on the signed narrowed packet coordinates. It calls the normal SDK packet initializers, GetTPage and GetClut, and returns the reloaded signed item count. No substitute rendering path is introduced.

The native menu caller801D3C4C previously omitted argument9. Retail explicitly stores1 atsp+20 before the call; the C caller now supplies it. Every annotated word in the saved caller assembly was checked against disc/menu.bin, including the stack stores and call. Other located shop/member-change callers already supply nine arguments.

Run `bash pc_port/tests/run_atlas_flip_retail_test.sh`. O0/O2/UBSan each pass67328 cases comparing every fixture byte and the return value against the retail function and actual retail SDK instructions. Native SDK bodies are extracted verbatim from the installed PsyCross source. Tests sweep all scale values, signed random geometry and screen coordinates, zero through four records, both packet buffers, local/global flips, byte truncation, zero-UV clipping, and sentinels. Six mutations are rejected: packet slot stride, missing byte truncation, missing vertical flip, negative rounding, reversed UV comparison, and missing width adjustment. An additional O2 run passes the exact captured atlas entry and nine arguments.

Scope limits: texture-page and CLUT inputs use hardware coordinate ranges. An initial arbitrary-coordinate sweep exposed signed-left-shift UB in PsyCross GetClut for negative Y; this SDK issue is not repaired or suppressed here. Geometry remains tested across signed/wrapping boundaries. This is not an exhaustive Cartesian test or proof of all malformed descriptors, aliased source/output buffers, hardware-rendered parity, or byte-for-byte compiled matching. Native linking and full PSX overlay build pass. The explicit compiled-byte comparison in compiled-bytes.json is FALSE after resolving SDK calls and local jumps to the retail address space. Fresh live execution of the repaired function is still pending. Full Lahan remains unproven. No commit or push.
